---
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: fitbook
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: backend

---
apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: fitbook
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
  selector:
    app: backend